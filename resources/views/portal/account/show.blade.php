@extends('layouts.app')
@section('title','Account | LandPay')
@section('body_class','admin-page')
@section('content')
<section class="admin-section"><div class="container site-container"><div class="admin-heading d-flex justify-content-between align-items-end"><div><span class="eyebrow eyebrow-dark">Client portal</span><h1>Account</h1><p>Contact information, contract details, and account security.</p></div><a class="btn btn-outline-brand" href="{{route('portal.dashboard')}}">Dashboard</a></div>@if(session('status'))<div class="alert alert-success mt-4">{{session('status')}}</div>@endif
<div class="row g-4 mt-2"><div class="col-lg-6"><div class="admin-next-card"><h2>Contact details</h2><p>{{$account->client->email}}<br>{{$account->client->primary_phone}}<br>{{$account->client->address_line_1}} {{$account->client->address_line_2}}<br>{{$account->client->city}} {{$account->client->state_region}} {{$account->client->postal_code}}</p>@if($pending)<div class="alert alert-info">A contact update is pending administrator review.</div>@endif<a class="btn btn-brand" href="{{route('portal.account.edit')}}">Request changes</a></div></div><div class="col-lg-6"><div class="admin-next-card"><h2>Security</h2><form method="post" action="{{route('portal.account.password')}}">@csrf @method('PUT')<label class="form-label">Current password</label><input class="form-control mb-2" name="current_password" type="password" required><label class="form-label">New password</label><input class="form-control mb-2" name="password" type="password" required><label class="form-label">Confirm new password</label><input class="form-control mb-3" name="password_confirmation" type="password" required><button class="btn btn-outline-brand">Update password</button></form></div></div></div>
<div class="admin-next-card mt-4"><h2>Contract details</h2><p class="text-muted">Current Payoff includes any unpaid base fee for the current billing month.</p><div class="table-responsive"><table class="table"><thead><tr><th>Plan</th><th>APN / Plan #</th><th>Start date</th><th class="text-end">Paid to date</th><th class="text-end">Current Payoff</th></tr></thead><tbody>@forelse($plans as $row)<tr><td>{{$row['plan']->title}}</td><td>{{$row['plan']->plan_number}}</td><td>{{$row['plan']->plan_start_date->format('M j, Y')}}</td><td class="money-cell">{{\App\Support\Money::format($row['paid_to_date'])}}</td><td class="money-cell">{{\App\Support\Money::format($row['current_payoff'])}}</td></tr>@empty<tr><td colspan="5">No active contracts.</td></tr>@endforelse</tbody></table></div></div></div></section>
@endsection
