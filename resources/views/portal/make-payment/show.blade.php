@extends('layouts.app')
@section('title','Payment notification | LandPay')
@section('body_class','admin-page')
@section('content')<section class="admin-section"><div class="container site-container"><div class="admin-heading"><div><span class="eyebrow eyebrow-dark">Client portal</span><h1>Payment notification sent</h1></div></div>@if(session('success'))<div class="alert alert-success mt-4">{{session('success')}}</div>@endif<div class="admin-next-card mt-4"><span class="dashboard-status {{$intent->status==='received'?'status-current':'status-due'}}">{{$intent->status==='received'?'Payment received':($intent->status==='checkout_pending'?'Payment processing':'Awaiting confirmation')}}</span><h2 class="mt-3">{{\App\Support\Money::format($intent->amount)}} by {{$method['name']}}</h2>@if($intent->status==='received')<p>Your payment has been received and posted to your account.</p>@elseif($intent->status==='checkout_pending')<p>Your card payment is being confirmed. Your account will update automatically when processing is complete.</p>@else<p>We have been notified that you intend to pay this amount. Your payment will be posted to your account as soon as it is received and confirmed by the administrator.</p>@endif<p><strong>Plan:</strong> {{$intent->paymentPlan->plan_number}}</p><a class="btn btn-outline-brand" href="{{route('portal.dashboard')}}">Return to dashboard</a></div></div></section>@endsection